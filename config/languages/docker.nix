{
  vim.languages.docker = {
    enable = true;
    lsp = {
      enable = true;
    };
    extraDiagnostics = {
      enable = true;
    };
    treesitter = {
      enable = true;
    };
    format = {
      enable = true;
    };
  };
}
